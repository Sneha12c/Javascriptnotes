"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using nodejs not browser

console.log( 9*
    8) // code readability should be high

    let x = -9;
    let swap = false;
    let value = "hijjek";
    let y = Math.abs(x);
    y = Math.E;
    let z = Math.ceil(-5);
    let m = Math.clz32(5); // no. leading zeros in binary representation
    let n = Math.fround(46.378989);
// datatypes
// int - range 1 to power(53)
// bigint =
// string = ""
// boolean = true/false
// undefined 
// null => standalonevalue
// 
console.log(y);
console.log(z);
console.log(n);
console.log(typeof null); // object
console.log(typeof undefined); // undefined

